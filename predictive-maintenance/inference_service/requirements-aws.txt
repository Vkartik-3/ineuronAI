# Corrected, minimal runtime deps for the AWS inference image.
# The legacy requirements.txt pulled tensorflow (unused) and omitted torch +
# redis (both required). This is the actually-correct set for the running app.
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
numpy>=1.24.3
scikit-learn>=1.3.2
pyyaml>=6.0.1
slowapi>=0.1.9
prometheus-client>=0.19.0
python-json-logger>=2.0.7
redis>=5.0.0
mlflow>=2.10.0
psycopg2-binary>=2.9.9
# kafka-python intentionally omitted: this inference deployment serves the
# HTTP prediction path only. With the client installed, the optional Kafka
# consumer blocks startup on DNS retries to an absent broker; without it the
# pipeline is skipped cleanly (ImportError caught in lifespan).
